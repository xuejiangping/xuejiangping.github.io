class GraphAdjMat {
  vertices: number[]
  adjMat: (0 | 1)[][]
  constructor(vertices: number[], edges: number[][]) {
    this.vertices = []
    this.adjMat = []
  }
  addVertex(val: number) {

  }
  addEdge(i: number, j: number) {
  }
}